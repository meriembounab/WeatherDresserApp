extends silayout

block content
  -data = data || {}

  if errors
    ul.my-errors
      for error in errors
        li= error.msg

  form(action="signin" method="POST" class="form-registration")
    label(for="email") Email:
    input(
      type="email"
      id="email"
      name="email"
      class="form-control"
      value=data.email
    )

    label(for="pass") Password:
    input(
      type="text"
      id="pass"
      name="pass"
      class="form-control"
      value=data.pass
    )

    input(
      type="submit"
      value="Sign In"
      class="btn btn-lg btn-primary btn-block"
    )
